import 'package:flutter/material.dart';

class My_reservationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(" My_reservations Page"),
      ),
    );
  }
}
